============
Installation
============

At the command line::

    $ pip install bids2datapackage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bids2datapackage
    $ pip install bids2datapackage
